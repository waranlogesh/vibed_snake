<!-- Deploy Guide: Create a Pusher Channels app at pusher.com (free tier). Copy your credentials into .env.local (see .env.local.example). Push to GitHub, import into Vercel, add the same env vars in Vercel project settings, and deploy. -->

# Vibed Snake

Real-time multiplayer Snake game. Create a room, share the 4-letter code, play!

## Getting Started

```bash
cp .env.local.example .env.local
# Fill in your Pusher credentials
npm install
npm run dev
```
